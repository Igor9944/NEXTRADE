# NexTrade Frontend

Frontend web application built with React, TypeScript, and Tailwind CSS.

## Available Scripts

In the project directory, you can run:

### `npm dev`

Runs the app in development mode.\
Open [http://localhost:5173](http://localhost:5173) to view it in the browser.

### `npm build`

Builds the app for production to the `dist` folder.

### `npm preview`

Locally preview the production build.

## Environment Variables

Create a `.env` file based on `.env.example` and fill in the values.

- `VITE_API_BASE_URL`: URL of the backend API
- `VITE_AI_SERVICE_URL`: URL of the AI service

## Learn More

To learn more about Vite, check the [Vite documentation](https://vitejs.dev/).

To learn more about React, check the [React documentation](https://reactjs.org/).

To learn more about TypeScript, check the [TypeScript documentation](https://www.typescriptlang.org/).

To learn more about Tailwind CSS, check the [Tailwind CSS documentation](https://tailwindcss.com/).